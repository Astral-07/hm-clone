# Database — update with your own credentials
spring.datasource.url=jdbc:postgresql://localhost:5432/hmclone
spring.datasource.username=postgres
spring.datasource.password=Fyr3Fly!

# JPA — auto-creates tables on startup in dev mode
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT secret (change this to a long random string in production!)
app.jwt.secret=hm-clone-super-secret-key-change-in-production-2024
app.jwt.expiration=86400000

# CORS — allow React dev server
app.cors.allowed-origins=http://localhost:5173
